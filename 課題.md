# nen-mcp-server 開発課題（ウィッシュリスト）

自作MCPサーバーのテスト運用で見えてきた、改善が必要なポイントのまとめだよ。

## 1. Windowsパスの正規化問題 (The `\\?\` Prefix)
- **現象**: `std::fs::canonicalize` を使用すると、Windows環境ではパスの先頭に拡張パス（UNCパス）を示す `\\?\` が付与される。これが原因で、一部のOS APIや日本語パスを含むファイル操作において `os error 123` (構文エラー) が発生する。
- **対策**: `dunce` クレートを使用して、可能な限り `\\?\` を含まない形式でパスを正規化するように変更することを推奨。

## 2. 標準入力（stdin）のエンコーディング不整合
- **現象**: PowerShellやコマンドプロンプトからパイプ経由でJSONリクエストを送る際、日本語（マルチバイト文字）が含まれるとエンコーディングの不一致（Shift-JIS vs UTF-8）でパスが壊れる。
- **対策**: サーバー側で標準入力の読み取りを強制的にUTF-8として扱うか、マルチバイト文字を含むパスのデコード処理を強化する。

## 3. CLIとしての独立動作（ハイブリッド化）
- **現象**: 現在はJSON-RPCを受け取る純粋なMCPサーバーとして動作するため、人間が直接コマンドラインから個別のツール（`safe_read`等）をテストするのが難しい。
- **対策**: `clap` 等を導入し、コマンドライン引数がある場合は通常のCLIツールとして動作し、引数がない場合はMCPサーバーとして待機するモードを設けると、テストが最強に楽になる。

## 4. エラーハンドリングの具体化
- **現象**: ファイル操作に失敗した際、`anyhow` でラップされた `os error` がそのまま返るため、原因（パスの書式、権限、ファイルの不在など）が分かりにくい。
- **対策**: ファイルシステムのエラーを自前のエラー型にマッピングし、より具体的な解決ヒント（例：「パスに無効な文字が含まれています」など）を返せるようにする。

## 5. 日本語ファイル名のパス解決
- **現象**: 日本語名を含むファイルに対して `safe_read` を呼び出すと、パスが解決できずエラーになることがある（上記1, 2の複合要因）。
- **対策**: 内部での `PathBuf` の扱いを見直し、Windows上でのユニコードパスの扱いに強い実装にアップデートする。
