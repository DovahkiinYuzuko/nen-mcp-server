# 変数関数仕様書

## 概要
`nen-mcp-server` は、文字化けに強いファイル読み取り機能を提供する MCP (Model Context Protocol) サーバーです。
CLI と MCP のハイブリッドモードをサポートしており、コマンドライン引数の有無で動作を切り替えます。

---

## 構造体・列挙型 (Structs & Enums)

### `Cli` (Struct)
コマンドライン引数を解析するためのメイン構造体。
- **依存関係**: `clap::Parser`
- **影響範囲**: アプリケーションのエントリポイント (`main`)
- **フィールド**:
  - `mcp` (bool): `true` の場合、MCP サーバーとして強制実行。
  - `command` (Option<Commands>): 実行するサブコマンド。

### `Commands` (Enum)
CLI モードで提供されるサブコマンドの定義。
- **依存関係**: `clap::Subcommand`
- **バリアント**:
  - `SafeRead`: 自動エンコーディング検知付きファイル読み取り。
  - `GetOutline`: Tree-sitter を使用したコードのアウトライン抽出。
  - `InspectFile`: ファイルメタデータとキーワード検索。
  - `ReadHex`: バイナリファイルのヘキサダンプ。

---

## 関数 (Functions)

### `main()`
プログラムのエントリポイント。
- **機能**: Windows コンソールの設定、引数の解析、MCP/CLI モードの分岐。
- **依存関係**: `Cli`, `fix_windows_console()`, `run_mcp_server()`, `handle_tool_call()`

### `fix_windows_console()`
Windows 環境でのコンソール出力を UTF-8 に固定する。
- **機能**: `chcp 65001` 相当の処理を Win32 API で実行。
- **依存関係**: `windows-sys`

### `handle_tool_call(name: &str, arguments: Value) -> CallToolResult`
ツール呼び出しを各実装に振り分けるディスパッチャ。
- **引数**:
  - `name`: ツール名。
  - `arguments`: JSON 形式の引数。
- **戻り値**: `CallToolResult` (MCP 互換のレスポンス)。
- **依存関係**: `tools` モジュールの各関数。
- **影響範囲**: MCP サーバーのループおよび CLI モードの出力。

### `run_mcp_server()`
標準入出力を通じた MCP サーバーのメインループを実行する。
- **機能**: JSON-RPC リクエストのパース、初期化、ツール一覧の提供、ツール実行。
- **依存関係**: `mcp` モジュール, `handle_tool_call()`

### `write_response(res_str: &str)`
標準出力に JSON-RPC レスポンスを書き込む。
- **引数**: `res_str`: レスポンス文字列。
- **機能**: 改行を付加してフラッシュする。
- **依存関係**: `std::io`
