# 変数関数仕様書

## 概要
NEN (Non-English Normalization) MCP Server の主要な変数、関数、およびその依存関係を管理する。

## 1. コアロジック (src/main.rs, src/mcp.rs)
(既存の仕様を維持)

## 2. アウトライン解析 (src/tools/get_outline.rs)

### 関数: `get_outline`
- **引数**: `path: &str` (解析対象ファイルのパス)
- **戻り値**: `Result<String>` (JSON形式のアウトライン結果)
- **依存関係**: `safe_read`, `get_language`, `parser::languages::get_definition_info`
- **影響範囲**: MCPツールの `get_outline` エンドポイント

## 3. パーサー・言語定義 (src/parser/)

### 関数: `get_language` (src/parser/mod.rs)
- **引数**: `extension: &str`
- **戻り値**: `Option<Language>`
- **説明**: 拡張子から Tree-sitter の言語オブジェクトを返す。

### 関数: `get_definition_info` (src/parser/languages/mod.rs)
- **引数**: `lang: &str`, `node: &tree_sitter::Node`, `source: &[u8]`
- **戻り値**: `Option<(String, String)>` (種類, 名前)
- **説明**: 言語グループごとのモジュール（`programming`, `web_scripts`, `configs_data`, `markup`）に処理を振り分け、ノードが定義かどうか、およびその名前を返す。
- **依存関係**: 各サブモジュールの `get_definition_info`
- **影響範囲**: `get_outline`

### サブモジュール: `programming`, `web_scripts`, `configs_data`, `markup` (src/parser/languages/*.rs)
- **役割**: 各パラダイムに属する言語の具体的なノード判定ロジックを保持する。
- **主要関数**: `get_definition_info(&tree_sitter::Node, &[u8]) -> Option<(String, String)>`
- **依存関係**: `tree-sitter` クレート

## 4. 依存関係マッピング

| コンポーネント | 依存先 | 影響を受けるコンポーネント |
| :--- | :--- | :--- |
| `get_outline` | `safe_read`, `get_language`, `languages::mod` | `mcp.rs` |
| `languages::mod` | `programming`, `web_scripts`, `configs_data`, `markup` | `get_outline` |
| `programming` | `tree-sitter` | `languages::mod` |
| `web_scripts` | `tree-sitter` | `languages::mod` |
| `configs_data` | `tree-sitter` | `languages::mod` |
| `markup` | `tree-sitter` | `languages::mod` |

---
*最終更新日: 2025-01-19*
